#include <bits/stdc++.h>
using namespace std;

#define pb push_back
#define fr first
#define sc second
#define sz(x) ((int) (x).size())
#define all(x) (x).begin(), (x).end()
#define forn(i, n) for (int i = 0; i < n; ++i)
#define fors(it, v) for (auto it = (v).begin(); it != (v).end(); ++it)

typedef pair<int,int> pii;
typedef long long ll;




int32_t main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    return 0;
}
